import AsyncStorage from '@react-native-async-storage/async-storage'

const readRedaction = async () => {
  try {
    const item = await AsyncStorage.getItem('Redaction')

    return item !== null ? JSON.parse(item) : []
  } catch (e) {}
}

export default readRedaction
